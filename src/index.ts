// ZarishSphere ui-vitals-entry microfrontend entry
import { version } from './version';

console.log('zs-ui-vitals-entry microfrontend starting, version:', version);

// Microfrontend bootstrap placeholder
export default version;
