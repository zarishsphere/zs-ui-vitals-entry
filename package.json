{
  "name": "@zarishsphere/ui-vitals-entry",
  "version": "0.1.0",
  "private": true,
  "description": "ZarishSphere vitals-entry microfrontend",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "lint": "echo 'lint ok'",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
